# Copyright (c) 2020 The ZMK Contributors
# SPDX-License-Identifier: MIT

# Bluetooth connection settings
CONFIG_BT_MAX_CONN=5
CONFIG_BT_MAX_PAIRED=5

# Windows-specific connection improvements
# Disables GATT subscription enforcement which fixes Windows Bluetooth issues
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n

# CRITICAL: Disable 2M PHY for Windows compatibility (fixes Realtek/Intel BT issues)
CONFIG_BT_CTLR_PHY_2M=n

# Enable passkey entry for Windows authentication
CONFIG_ZMK_BLE_PASSKEY_ENTRY=y

# Increase BLE transmit power for better connectivity
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Enable auto-discovery of Client Characteristic Configuration for better Windows compatibility
CONFIG_BT_GATT_AUTO_DISCOVER_CCC=y

# Setup combo limit to allow new combo setting to work properly 
CONFIG_ZMK_COMBO_MAX_COMBOS_PER_KEY=16
CONFIG_ZMK_COMBO_MAX_KEYS_PER_COMBO=8
CONFIG_ZMK_COMBO_MAX_PRESSED_COMBOS=8

# Enable USB for troubleshooting and fallback
CONFIG_ZMK_USB=y

# Power management settings
CONFIG_ZMK_IDLE_TIMEOUT=30000
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=1800000

# Mouse/pointing support (updated from ZMK_MOUSE to the current config name)
CONFIG_ZMK_POINTING=y

<<<<<<< HEAD
# Connectivity issue
# Increase BLE transmit power of nRF52840
# value PLUS_8 can be set to any multiple of four between MINUS_20 and PLUS_8
# https://zmk.dev/docs/troubleshooting

CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# Windows-specific Bluetooth fixes
# Disable 2M PHY for better compatibility with Windows (Intel/Realtek chips)
CONFIG_BT_CTLR_PHY_2M=n

# Disable GATT enforcement that causes issues with Windows
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n


# Added line to enable support for mouse control

CONFIG_ZMK_MOUSE=y
=======
# Mouse support enabled
# Remove speed multipliers to use defaults
>>>>>>> 845d8f627d9d89e5b55446960b437bae5bd39d05
